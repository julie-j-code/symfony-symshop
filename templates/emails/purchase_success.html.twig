<h1>Confirmation de votre commande n°{{ purchase.id }} effectuée le {{ purchase.purchasedAt | date('d/m/Y à H:i') }}</h1>
<h2> Détails de la commande :</h2>
<p>Merci {{ currentUser.fullName }} pour votre commande ! Vous pouvez retrouver tous les détails ci-dessous.</p>
<table>
    <thead>
        <tr>
            <th>Produit</th>
            <th>Quantité</th>
            <th>P. U.</th>
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        {% for item in purchase.purchaseItems %}
            <tr>                    
                <td>{{ item.productName }}</td>
                <td>{{ item.quantity }}</td>
                <td>{{ item.productPrice / 100}}€</td>
                <td>{{ item.total / 100 }}€</td>
            </tr>
        {% endfor %}
    </tbody>
    <tfoot>
        <tr>
            <th colspan="3">Prix Total</th>
            <td>{{ purchase.total / 100}}€</td>
        </tr>
        <tr>
            <th colspan="3">État de la commande</th>
            <td>{{ purchase.status }}</td>
        </tr>
        <tr>
            <th>Adresse de livraison</th>
            <td colspan="3">{{ purchase.address }},<br>{{ purchase.postalCode }} {{ purchase.city }}</td>
        </tr>
    </tfoot>
</table>